---
const benefits = [
  {
    icon: `<svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
           </svg>`,
    title: "Seguridad de Datos",
    description: "Protección avanzada con encriptación de extremo a extremo y copias de seguridad automáticas."
  },
  {
    icon: `<svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 5.636l-3.536 3.536m0 5.656l3.536 3.536M9.172 9.172L5.636 5.636m3.536 9.192L5.636 18.364M12 12h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
           </svg>`,
    title: "Soporte 24/7",
    description: "Equipo técnico disponible las 24 horas para resolver cualquier consulta o incidencia."
  },
  {
    icon: `<svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
           </svg>`,
    title: "Optimización de Tiempo",
    description: "Automatización de procesos rutinarios para que dediques más tiempo a tus pacientes."
  },
  {
    icon: `<svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
           </svg>`,
    title: "Control Administrativo",
    description: "Panel de control completo con métricas, reportes financieros y análisis de rendimiento."
  }
];
---

<section class="py-20 bg-white">
  <div class="container mx-auto px-6">
    <div class="text-center mb-16 section-animate">
      <h2 class="text-4xl md:text-5xl font-bold text-gray-800 mb-6">
        ¿Por Qué Elegir <span class="text-primary-600">Kenkomed</span>?
      </h2>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto leading-relaxed">
        Más de 500 clínicas confían en nuestro sistema para optimizar sus operaciones 
        y brindar una mejor experiencia a sus pacientes.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 mb-16">
      {benefits.map((benefit, index) => (
        <div class="group benefit-card section-animate text-center"
             style={`animation-delay: ${index * 0.1}s;`}>
          <div class="w-20 h-20 bg-gradient-to-br from-primary-500 to-medical-500 rounded-2xl flex items-center justify-center text-white mx-auto mb-6 group-hover:scale-110 transition-transform duration-300 group-hover:shadow-lg">
            <Fragment set:html={benefit.icon} />
          </div>
          
          <h3 class="text-xl font-bold text-gray-800 mb-4 group-hover:text-primary-600 transition-colors">
            {benefit.title}
          </h3>
          
          <p class="text-gray-600 leading-relaxed">
            {benefit.description}
          </p>
        </div>
      ))}
    </div>

    <!-- Stats Section -->
    <div class="section-animate">
      <div class="bg-gradient-to-r from-primary-600 to-medical-600 rounded-3xl p-12 text-white text-center">
        <h3 class="text-3xl font-bold mb-8">Nuestros Números Hablan</h3>
        
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
          <div class="stats-item">
            <div class="text-4xl font-bold mb-2 counter" data-target="500">0</div>
            <div class="text-blue-100">Clínicas Activas</div>
          </div>
          
          <div class="stats-item">
            <div class="text-4xl font-bold mb-2 counter" data-target="15000">0</div>
            <div class="text-blue-100">Pacientes Registrados</div>
          </div>
          
          <div class="stats-item">
            <div class="text-4xl font-bold mb-2 counter" data-target="98">0</div>
            <div class="text-blue-100">% Satisfacción</div>
          </div>
          
          <div class="stats-item">
            <div class="text-4xl font-bold mb-2 counter" data-target="24">0</div>
            <div class="text-blue-100">Horas de Soporte</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // Counter animation
  function animateCounters() {
    const counters = document.querySelectorAll('.counter');
    counters.forEach(counter => {
      const target = parseInt(counter.getAttribute('data-target'));
      const duration = 2000;
      const step = target / (duration / 16);
      let current = 0;
      
      const timer = setInterval(() => {
        current += step;
        if (current >= target) {
          current = target;
          clearInterval(timer);
        }
        counter.textContent = Math.floor(current).toLocaleString();
      }, 16);
    });
  }

  // Trigger animation when in view
  const statsSection = document.querySelector('.stats-item');
  const statsObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        animateCounters();
        statsObserver.unobserve(entry.target);
      }
    });
  });

  if (statsSection) {
    statsObserver.observe(statsSection);
  }
</script>